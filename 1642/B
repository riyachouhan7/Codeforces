#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define endl '\n'
int main(){
    ll t;cin>>t;
    while(t--){
        int n;
        cin >>n;
        set<int> v;
        for (int i = 0; i < n; i++){
            int a;
            cin>>a;
            v.insert(a);
        }
        int s=v.size();
        for (int i = 1; i <= n; i++){
            cout << max(s,i)<<" ";
        }
        cout << '\n';
    }
    return 0;
}
